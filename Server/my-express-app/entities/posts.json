{
  "post_id": "INT PRIMARY KEY AUTO_INCREMENT",
  "user_id": "INT",
  "title": "VARCHAR(255)",
  "content": "VARCHAR(255)",
  "date": "DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP",
  "__constraints": ["FOREIGN KEY (user_id) REFERENCES users(user_id)"]
}
